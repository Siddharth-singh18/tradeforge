version: '3.8'

services:
  mongo:
    image: mongo:7
    ports: 
      - "27018:27017"
    volumes: 
      - mongo_data:/data/db

  redis:
    image: redis:7-alpine
    ports: 
      - "6388:6379"

volumes:
  mongo_data:
